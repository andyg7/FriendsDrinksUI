---
version: '3.7'

services:
  friendsdrinks-ui:
    image: andyg001/friendsdrinksui:latest
    volumes:
      - ${PWD}/server/config.properties:/app/config/config.properties
    ports:
      - "8080:8080"
